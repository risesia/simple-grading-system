#include "course.h"
#define ARRAY_MAX 100

// string student[] = { "" };

string course::inputCourse(string courseName, int countCourse)
{
    string courseArr[ARRAY_MAX] = { "" };
    // string** student[ARRAY_MAX] = new string*[ARRAY_MAX];

    courseArr[countCourse] = courseName;

    return string();
}